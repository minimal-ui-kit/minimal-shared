import eslintConfig from 'internal-eslint-config';

// ----------------------------------------------------------------------

/**
 * ESLint Flat Configuration
 * This package inherits the shared configuration from '@mindtrust/eslint-config'.
 *
 * @type {import("eslint").Linter.Config[]}
 */
export default eslintConfig;
